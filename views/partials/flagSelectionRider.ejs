<h4 class="toh-mb-18">Rider Flag Number</h4>
<div style="padding-left: 60px; padding-right: 60px;">
  <div class="toh-mb-18">
    <div class="toh-flag-mockup uk-align-center">
      <div id="flagNumberText" class="toh-flag-number">
        <%= User.FlagNumber %> 
      </div> 
    </div>
  </div>
  <% if(User.FlagNumber > 10) { %>
    <div id="keepExistingFlagNumSection">
      <p>Would you like to keep this flag number again for next year?</p>
      <button type="button" id="chooseAnotherFlagNumRider" class="uk-button uk-button-primary uk-buttom-small button-rounded chooseAnotherFlagNum"d ata-whoami="rider" data-userid="<%= User.id %>">No</button>
      <button type="button" id="keepExistingFlagNumRider" class="uk-button uk-button-primary uk-buttom-small button-rounded" data-userid="<%= User.id %>" data-existingriderflagnum="<%= User.FlagNumber %>">Yes</button>
    </div>
  <% } else { %>
    <% if(User.FlagNumber <= 10) { %>
      <p>You currently have a reserved Flag Number. Please click the button below to choose a flag number.</p>
    <% } %>
    <% if(User.FlagNumber == 0) { %>
      <p>Welcome to the rally! Please click the button below to choose a flag number.</p>
    <% } %>
    <button type="button" id="chooseAnotherFlagNumRider" class="uk-button uk-button-primary uk-buttom-small button-rounded chooseAnotherFlagNum" data-whoami="rider" data-userid="<%= OrderInfo.PassUserID %>">No</button>
  <% } %>
</div>